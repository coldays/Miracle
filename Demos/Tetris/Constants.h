#pragma once

#include <string>
#include <iostream>
#include <cstdlib>

const float zIndexGui = 0;
const float zIndexBlocks = 1;
const float zIndexMap = 2;

const float BlockSpawnY = 9;

const std::string BaseFolderPath = std::string(std::getenv("APPDATA")) + "\\MiracleTetris\\";